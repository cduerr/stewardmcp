{
  "name": "stewardmcp",
  "version": "1.0.2",
  "description": "MCP server that gives Claude Code instances a dedicated, context-aware engineer for any repo",
  "type": "module",
  "bin": {
    "stewardmcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "prepare": "tsc"
  },
  "dependencies": {
    "@anthropic-ai/claude-code": "^1",
    "@modelcontextprotocol/sdk": "^1"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5"
  },
  "files": [
    "dist",
    "defaults"
  ]
}
